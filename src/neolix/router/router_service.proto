// Defines the GRPC interfaces of the New Routing Service.
syntax = "proto2";
package adu.common.router;

import "router.proto";

// The signature of the RPC service, provided by the Decision module.
service RPC {
    rpc RunRouter(RoutingRequest) returns (RoutingResult) {}
}
