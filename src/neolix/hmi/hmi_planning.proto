// Defines the message about planning demand.
syntax = "proto2";
package adu.hmi;

option java_multiple_files = true;

import "hmi_common.proto";

// The Unit planning traj message
message PlanningObject {
    optional PolygonPoint point = 1;    // planning position point
    optional double heading = 2;        // heading of planning traj
}